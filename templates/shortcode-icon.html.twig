{#
/**
 * @file
 * Default theme implementation for the icon shortcode.
 *
 * Available variables:
 * - shape
 * - color
 * - size
 * - wrapper
 */
#}
<i{{ create_attribute({ class: [shape, color ? 'ucb-icon-color-' ~ color, size, pull ? 'fa-pull-' ~ pull, wrapper ? 'ucb-icon-style-' ~ wrapper] }) }}>&nbsp;</i>
